# Document OCR API Configuration
# Copy this file to .env and fill in your actual values

# OCR Request Authentication Secret
# Change this to a strong secret in production
OCR_SECRET_KEY=your-ocr-secret-key-here-change-in-production

# Webhook Forwarder Configuration (Optional)
# Leave empty if not using webhook forwarding feature

# Secret key for signing forwarded webhooks
FORWARDER_SECRET=your-forwarder-secret-here

# Webhook target endpoints (up to 3 targets)
FORWARDER_TARGET_1_URL=https://endpoint1.example.com/webhook
FORWARDER_TARGET_2_URL=https://endpoint2.example.com/webhook
FORWARDER_TARGET_3_URL=https://endpoint3.example.com/webhook

# OCR Engine Configuration (Optional)
# Set to 1 to enable EasyOCR fallback if RapidOCR is unavailable
ENABLE_EASYOCR_FALLBACK=0

# Universal OCR Document Input (Optional)
# Applies to multipart uploads and documents downloaded from URL
DOCUMENT_MAX_UPLOAD_BYTES=20971520
DOCUMENT_INPUT_SPOOL_MEMORY_BYTES=1048576
DOCUMENT_URL_ENABLED=1
# HTTPS is required unless this is explicitly enabled
DOCUMENT_URL_ALLOW_HTTP=0
DOCUMENT_URL_ALLOWED_PORTS=443
# Optional comma-separated exact hosts or wildcard subdomains, e.g. files.example.com,*.storage.example.com
DOCUMENT_URL_ALLOWED_HOSTS=
DOCUMENT_URL_CONNECT_TIMEOUT_SECONDS=3
DOCUMENT_URL_READ_TIMEOUT_SECONDS=8
DOCUMENT_URL_TOTAL_TIMEOUT_SECONDS=20
DOCUMENT_URL_MAX_REDIRECTS=3
DOCUMENT_URL_MAX_LENGTH=2048

# Business Document OCR Limits (Optional)
# Maximum PDF pages and upload bytes processed by /api/business-document
BUSINESS_DOCUMENT_MAX_PAGES=20
BUSINESS_DOCUMENT_MAX_UPLOAD_BYTES=20971520
# Reject decoded images/PDF page renders above this pixel budget
BUSINESS_DOCUMENT_MAX_IMAGE_PIXELS=25000000
# Bound retained OCR/selectable text independently for each page
BUSINESS_DOCUMENT_MAX_PAGE_TEXT_CHARS=100000
# Compare selectable PDF text with rendered OCR when embedded page text is weak
BUSINESS_DOCUMENT_COMPARE_RENDERED_PDF_TEXT=1

# Flask Configuration (Optional)
# FLASK_ENV=development
# FLASK_DEBUG=1

